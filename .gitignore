__pycache__/
*.pyc
.venv/
.streamlit/secrets.toml
src/params_real.py
.DS_Store
node_modules/
dist/
# Real (non-sanitized) numbers must NEVER reach a committed/public artifact.
# `dist/` is ignored wholesale above; this line keeps the guardrail explicit and
# survives even if `dist/` is ever un-ignored for the sanitized bundle.
dist/real/
# Cached raw price pulls from Energy-Charts (regenerated by the loader).
engine/data/cache/
